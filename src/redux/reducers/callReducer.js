const INS = {
    onCall: false,
    myStream: {},
    thierStream: {},

}

export default (state = INS, action) => {

    switch (action.type) {
        case 'dsa':

            break;

        default:
            return state
    }

}
